import useTitle from "../hooks/useTitle"

const CreateWorkoutPage = () => {
  useTitle("Create a Workout | GymNation")
  return (
    <div>CreateWorkoutPage</div>
  )
}

export default CreateWorkoutPage